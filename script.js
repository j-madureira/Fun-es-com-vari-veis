/*Crie uma função para o usuário digitar algo
*/

alert(`Bem vindo, digite alguma coisa!`);

function digitar(digito){
    var digitado = digito;
    console.log(digitado);
}
digitar(prompt(`Digite alguma coisa`));